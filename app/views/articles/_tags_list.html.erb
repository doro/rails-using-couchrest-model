<div class="tags-list">
<% if tags_list.length > 0 %>
tags: 
<% end %>
<% tags_list.each do |tag| %>
  <span><%= link_to "#{tag.key} [#{tag.value}]", "/articles/tag/#{tag.key}" %></span>
<% end %>
</div>
